# Course 03 Notes

Agent = Prompt + LLM Decision + Tool / Environment Interaction + State + Loop Control.

The Runtime assembles context, executes tools, records observations, updates state, and decides whether the loop should continue or stop.

